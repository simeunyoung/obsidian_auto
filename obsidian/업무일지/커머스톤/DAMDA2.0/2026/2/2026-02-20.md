
#### git branch 정리

```
// 로컬 브랜치 삭제
git branch | grep -E "feature/|chore/" | xargs git branch -D

// 원격 브랜치 삭제
git branch -r | grep -E "origin/feature/|origin/chore/" | sed 's/origin\///' | xargs -I {} git push origin --delete {}

// 원격 추적 브랜치 정리
git fetch -p

```