
기존 외부 호출 경로는 다음과 같다.

`API Gateway → internal ALB(HTTPS listener) → Target Group(HTTP) → EC2`

문제는 내부 호출도 외부와 유사하게 `EC2 → 위 구조` 로 타고 있었다는 점이다. 이 경우 내부 서비스 간 호출인데도 ALB의 HTTPS listener를 거치게 되며, 호출 주체가 ALB의 기본 ELB 도메인으로 접근하면 인증서 이름과 